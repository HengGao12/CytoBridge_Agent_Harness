---
title: "Downstream plot APIs"
summary: "Compatibility pointer to the runtime downstream API reference."
read_when:
  - "Creating or validating downstream plots"
---
# Downstream Plot APIs

The canonical downstream plotting documentation now lives in:

- `docs/runtime/downstream/api-reference.md`
- `docs/runtime/downstream/recipes-and-artifacts.md`

Use those pages for plot API contracts and report-facing artifact rules. This
compatibility page is intentionally short to avoid maintaining duplicate
downstream plot documentation.
