---
title: "Downstream compute APIs"
summary: "Compatibility pointer to the runtime downstream API reference."
read_when:
  - "Calling or checking downstream compute APIs"
---
# Downstream Compute APIs

The canonical downstream API documentation now lives in:

- `docs/runtime/downstream/README.md`
- `docs/runtime/downstream/api-reference.md`
- `docs/runtime/downstream/recipes-and-artifacts.md`

Use `docs/runtime/downstream/api-reference.md` for exact compute and plotting
APIs. This compatibility page is intentionally short to avoid maintaining two
copies of the same API contract.
