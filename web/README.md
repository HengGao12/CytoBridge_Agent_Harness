# CellCompass Frontend

This is a neutral React + Vite workspace prepared for visual redesign.

## Run

```bash
npm install
npm run dev
```

## Scope

Focus on layout, spacing, typography, color, component hierarchy, responsive behavior, and interaction polish.

Keep API routes, event payload handling, and runtime field names stable unless a UI-only change clearly requires a local rename.
